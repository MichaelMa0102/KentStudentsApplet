<view class="container {{type == 'ranking' ? 'container--ranking' : ''}}">
  <!-- 图片展示区域 -->
  <view class="image-section" wx:if="{{imageUrl}}">
    <image src="{{imageUrl}}" mode="aspectFill" class="detail-image"></image>
  </view>
  <view class="image-placeholder" wx:if="{{type == 'ranking' && !imageUrl}}">
    <text class="placeholder-text">学校详情</text>
    <text class="placeholder-hint">内容待完善</text>
  </view>

  <!-- 内容区域 -->
  <view class="content-section">
    <view class="title-section">
      <text class="detail-title">{{title}}</text>
      <text class="detail-type">{{typeText}}</text>
    </view>

    <view class="divider"></view>

    <!-- 描述信息 -->
    <view class="info-section" wx:if="{{description}}">
      <text class="info-label">描述：</text>
      <text class="info-content">{{description}}</text>
    </view>

    <!-- 详细内容 -->
    <view class="content-section-inner" wx:if="{{content}}">
      <text class="content-label">详细内容：</text>
      <text class="content-text">{{content}}</text>
    </view>

    <!-- 返回按钮 -->
    <view class="button-section">
      <button class="back-button" bindtap="goBack">返回首页</button>
    </view>
  </view>
</view>
